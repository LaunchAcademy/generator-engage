{
  "scripts": {
    "prod": "node <%= appPath %>"
  },
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "name": "<%= name %>-server",
  "engines": {
    "node": "^<%= nodeVersion.major %>.<%= nodeVersion.minor %>"
  }
}
